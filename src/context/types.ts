export type CartItem = {
  productId: number;
  name: string;
  price: number;
  image: string;
  quantity: number;
};

export type User = {
  id: number;
  name: string;
  email: string;
  address: string;
  birthday: string;
};
